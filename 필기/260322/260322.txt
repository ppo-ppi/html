$웹표준 규칙으로 정해짐.
$인라인 요소는 블럭요소를 자식으로 가질 수 없다.
$블럭요소는 인라인 요소를 자식으로 가질 수 있다.

$ 인라인은 글씨같은것.
$span는 인라인 태그 span도 div처럼 의미가 없다.
span 태그도 구획을 나눌때 사용한다. <span> 황소희 </span>님 환영합니다. 

$인라인 태그의 종류
-a -target
- img = 자식태그가 없어서 끝태그가 없다. 속성으로 제어한다.
이미지를 넣는 방법
1) div에 css로 넣기
2) img로 넣기.
==> alt에 이미지 설명을 넣기
3) 인라인태그는 너비가 높이가 없지만, 이미지태그는 예외이다. => 그리고 width만 줘도
비율에 맞춰서 조정된다. 하지만 가로, 세로를 동시에 주면 비율이 일그러진다. 하나만 씀. 
그래서 왠만하면 너비에 맞춰서 쓴다.
- input태그 = 인라인 태그
=> input button -> value에 글씨 넣기. button이랑 input으로 통일하는 추세이긴 함.
--> 페이지를 이동할때는 button태그를 쓰지 않는다. -> button밑에 a태그가 있어서 더블충돌이 발생한다.
span{내용} tab = 

$실습
class 여러개 넣기 .content-wrap.cnt-wrap => 이렇게

$>을쓰면 자식관계태그를 바로 만들 수 있다.
(li>a)*3

$+를 쓰면 형제 관계를 만들 수 있다.
h4>lorem4 하면 h4만들고 내용으로 lorem 바로 쓸 수 있음!

div 안에 img를 넣으면 div구획만큼 잘라줄 수 있다.
https://special-zebra-r7w9xpx6w4vhpj45-5500.app.github.dev/html/webfonts/fa-solid-900.woff2
