가로는 바뀌기때문에 20px이상은 숫자로 넣지않고 calc로 계산해서 넣는다. 20정도는 괜찮

아트탑 텍스트 패딩 왼쪽 계산하는 방법 -> TEXT => calc(100% / ) => width를 준 적 없기때문에 100%는 아티클이다.

display: flex는 width값이 없으면 원래의 가로 길이만큼만 차지한다.
👉 calc() 안의 %는 항상 부모 기준 퍼센트라고 보면 된다. => 내 자신의 width가 아니다!
부모에 width값이 설정되어있던지 말던지 아무튼 부모 기준이다.
전임자가 공통으로 쪼개놓은 선택자들을, 더 깊은 선택자를 작성해서 덮는다.
flex하면 높이가 큰쪽 높이로 잡힌다.

공통화 = 공통화된게 많은것을 넣고, 특별한애는 따로 집어서 넣어준다.

텍스트 => 줄수 제한하는방법 line-height와 height 그리고 overflow-hidden 써야한다.
height: 
버튼은 라인하이트도, 하이트도 되지만 a태그로 대체될대를 대비해서 lh를 준다.

부모보다 너비가 작으면 width값을 줘야한다.
wrap보다 안에있는 grid가 작아야 가운데 정렬이 가능하다. 그래서 width값이 있을때만 가능하다.

헤더에 각각 
.content-wrap2>.cnt2>header>h3{
    /* line-height: 20px;
    height: 40px;
    overflow: hidden; */
    width: calc(80% / 12 * 6);
    margin: 0 auto;
    max-width: 520px; => 
}

웹에서 최소사이즈는 12px 그 이하로 가면 모바일에 가서 보면 너무 작음..

이미지를 백그라운드로 써야할때 쓸거. reset에 넣어도 좋음..
    background-size: cover;
    background-repeat: no-repeat;
    background-position: center;

    .content-wrap>.content>section article .image {
    background-size: cover;
    background-repeat: no-repeat;
    background-position: center;
    color: transparent; => 텍스트 안보이게
    font-size: 1px; 
}

.content-wrap2>.cnt2>section>article>.image>video{
    height: 100%; // 가로 세로 둘다 줘도 되는데 큰것만 하나 주기.
    object-fit: cover;
}

미디어쿼리
컨텐츠 3
플렉스의 특징은 가로값을 줘도 알아서 배열한다..
-> 

플렉스랩 = 플렉스 -> 알아서 나누기.
자식이 합쳤을때 부모 100%만 차지하게 하고, 나머지는 다음줄로 나열하기.
flex-wrap을 넣어주면 값이 절대적으로 바뀐다. 
flex는 알아서 줄여서 넣어준다.. => 요소들이 줄여진다!
flex-wrap은 부모 가로값이 넘기면 다음줄로..