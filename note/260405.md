display:flex를 꼭 써야 쓸수있는 속성들 (flex에 종속)
플렉스 => 높이를 안줘도 자동으로 높이가 큰쪽에 높이로 적용된다.

 justify-content: space-around; // 남아있는 공백을 균일하게 분할한다. (양쪽끝에 공백생김)
  justify-content: space-between;  // 양쪽 끝.. => 줄여도 밀리지않는다.
 
 마진값을 준다.

 동일한 사이즈의 애들은 => width값이 필요하지 않다. 만약 길어지거나 깨지면 width값을 넣어야한다.
  width값이 꼭 필요한경우 => 영역을 구분해야 할때. 

  개발자도구에서 스타일은 우선순위 위주로 나온다.
  
  공통적으로 들어가는 텍스트부터 높이를 준다.

  ovh = 높이가 있어야 작동한다. 막 겹치는건 높이 이상으로 텍스트가 들어왔을때 넘치기 때문.

2) 글자들 높이 설정, 
  .content-wrap > .content > section article > .texts > p {
    height: 80px;
    line-height: 20px;
}

3) 이미지 높이 설정

4) 정렬
=> text-align=center 도 상속된다.

5) 가로 값 설정(그리드 계산)

5) 폰트 사이즈 설정

link => html에서 가져올때 사용
스타일 태그(css) 안에서 가져올때 사용 => @import


font-family: 'Funnel Sans', '', ''; => 차례대로 적용하고 폰트가 없으면 기본폰트로 적용된다.폰트가 없으면 적용안함(아이폰은..)
font-weight: 700; => 두꺼운 글씨는 700으로 주로 쓴다.

접근성 => 이미지 위에 폰트를 숨기고 폰트사이즈를 1로 맞춘다.

article {
    transition: margin-top 0.3s;
}

article:hover {
    margin-top: -10px;
}


미디어 쿼리는 빼거나 감추는 작업이 주로..

레이아웃 작업
1) 그리드 잡기 **** (중요)
2) 정렬하기
3) 정렬안 궈터, 스페이스 주기
4) 마진, 패딩 설정
4) 높이 주기
5) 너비 주기 **** (중요)
6) 백그라운드 컬러 주기
7) 폰트 속성 주기(가운데, 사이즈, 굵기, 스타일..)
8) 이미지, 비디오 등 주기..